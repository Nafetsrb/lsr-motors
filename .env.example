# Base de données (docker compose utilise cette valeur automatiquement)
DATABASE_URL="postgresql://lsrmotors:lsrmotors_dev@localhost:5432/lsrmotors"

# NextAuth — générer avec : openssl rand -base64 32
NEXTAUTH_SECRET="changeme"
NEXTAUTH_URL="http://localhost:3000"

# Dossier d'upload des médias
UPLOAD_DIR="./uploads"
